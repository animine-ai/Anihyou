name: Context boundary

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Verify centralized AI context boundary
        shell: bash
        run: ./scripts/verify-context-boundary.sh
